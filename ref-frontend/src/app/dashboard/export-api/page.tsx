import { ExportApiHub } from '@/components/export-api/ExportApiHub'

export const metadata = {
  title: 'Export & API — Colytics AI',
  description: 'Export data, schedule reports, and integrate Colytics into your infrastructure via REST API.',
}

export default function ExportApiPage() {
  return <ExportApiHub />
}
